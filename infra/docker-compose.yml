services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    networks:
      - n8n-network
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-goal_tracker}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "55432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d ${POSTGRES_DB:-goal_tracker}"]
      interval: 10s
      timeout: 5s
      retries: 5

  api:
    container_name: bughoi-api
    build:
      context: ..
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    networks:
      - n8n-network
    environment:
      API_PORT: 4000
      DATABASE_URL: ${DATABASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      N8N_API_TOKEN: ${N8N_API_TOKEN}
      OWNER_EMAIL: ${OWNER_EMAIL}
      OWNER_PASSWORD: ${OWNER_PASSWORD}
      OWNER_DISPLAY_NAME: ${OWNER_DISPLAY_NAME}
    depends_on:
      postgres:
        condition: service_healthy

  web:
    container_name: bughoi-web
    build:
      context: ..
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    networks:
      - n8n-network
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL}
    depends_on:
      - api

networks:
  n8n-network:
    external: true
    name: n8n-docker-caddy_n8n-network

volumes:
  postgres_data:
